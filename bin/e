#!/bin/env fish

if test -n "$NVIM"
    if test -n "$argv"
        nvim --server $NVIM --remote $(realpath $argv)
    else
        nvim --server $NVIM --remote .
    end
else
	set target "."
	test -n "$argv" && set target $argv
	nvim $target
end
